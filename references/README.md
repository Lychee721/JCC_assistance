# References

这个目录只放外部参考资料，不放项目主代码。

当前包含：

- `academy-library-upstream/`
  上游 `tacticians-academy/academy-library` 的本地参考副本，用来研究它的 ETL 和数据组织方式。

说明：

- 顶层仍然留有旧目录 `_reference_academy_library/`，那是早期临时拉下来的参考副本。
- 由于本机权限限制，当前没有直接重命名成功，所以新增了更清晰的 `references/academy-library-upstream/` 副本作为正式参考目录。
- 后续你查看参考实现时，优先看 `references/academy-library-upstream/`。
